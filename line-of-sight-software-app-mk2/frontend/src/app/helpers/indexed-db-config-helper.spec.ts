import { IndexedDbConfigHelper } from './indexed-db-config-helper';

describe('IndexedDbConfigHelper', () => {
  it('should create an instance', () => {
    expect(new IndexedDbConfigHelper()).toBeTruthy();
  });
});
