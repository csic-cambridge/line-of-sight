import { RequiredNameValidator } from './required-name-validator';

describe('RequiredNameValidator', () => {
  it('should create an instance', () => {
    expect(new RequiredNameValidator()).toBeTruthy();
  });
});
